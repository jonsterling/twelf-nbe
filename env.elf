env : type.
· : env.
; : env -> sem -> env. %infix right 8 ;.

%abbrev ctx = env.

length : env -> nat -> type.
%mode length +R -N.

- : length · ze.
- : length (R ; _) (su N)
     <- length R N.

%worlds () (length _ _).
%total R (length R _).