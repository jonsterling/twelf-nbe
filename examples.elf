%define welp = Z
%solve - : quote (Σ 1 ([x] (Σ 1 ([y] x)))) Z.

%define result = Z
%solve test : nbe (pi unit ([x] unit)) (lam [x] x) Z.

%solve - : chk (fst / pair ax ax) unit.
%solve test2 : chk
                (lam [x] fst / x)
                (pi (sg unit ([x] unit)) ([z] unit)).